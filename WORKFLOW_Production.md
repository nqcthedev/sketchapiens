# WORKFLOW SẢN XUẤT — CHUẨN PRODUCTION
*Chốt 29/07/2026. Thay cho quy trình cũ (viết kịch bản trước → thumbnail sau).*

> ## ⚠️ THAY ĐỔI LỚN NHẤT: PACKAGING ĐI TRƯỚC KỊCH BẢN
>
> Quy trình cũ: chọn đề tài → viết kịch bản → chia shot → gen ảnh → TTS → ghép → **rồi mới làm thumbnail**.
>
> Sai ở đâu: thumbnail là thứ quyết định, nhưng lại được làm cuối cùng, khi đã tiêu hết công và không còn đường lùi.
>
> **Bằng chứng** (`NGHIENCUU_ThiNghiem_BaySinhDoi.md`): 20 video cùng title cùng tháng, winner 59.630 vs 6.286. **Tỉ lệ like ngang nhau (3,21% vs 3,91%)** — người đã xem thì thích như nhau. Toàn bộ chênh lệch nằm trước cú bấm.
>
> → **Chốt CONCEPT thumbnail trước khi viết một chữ kịch bản.** Concept không qua cửa thì đổi đề tài, chưa mất gì.
>
> ### 🔧 SỬA 04/08/2026 — tách CONCEPT khỏi ẢNH THẬT
> Luận điểm "packaging đi trước" vẫn đúng, nhưng **gen ảnh** ở giai đoạn 1 thì sai: lúc đó chưa có gì để đối chiếu nên phải mò. V18 mất **7 vòng**, rồi tới lúc gen ảnh video mới lòi ra luật nhân vật thật và phải quay lại sửa.
> **Nay: cửa 1 chỉ chấm concept (10 phút). Ảnh thật dựng ở giai đoạn 5a, sau khi đã có đủ ảnh video để lấy khối nhân vật y nguyên.**

---

# SƠ ĐỒ

```
GIAI ĐOẠN 0  CẦU & CUNG        →  cửa 0: có cầu? bầy clone mỏng?
GIAI ĐOẠN 1  CHỈ TITLE            →  cửa 1: khuôn title + nối hành vi hôm nay
GIAI ĐOẠN 2  KỊCH BẢN           →  cửa 2: rubric ≥62/72 + verify mỏ neo
GIAI ĐOẠN 3  SHOT & ẢNH         →  cửa 3: đếm + đo nền
GIAI ĐOẠN 4  TIẾNG & GHÉP       →  cửa 4: đối chiếu 9 mốc
GIAI ĐOẠN 5a THUMBNAIL THẬT     →  cửa 5a: chất liệu · trắng · cắt mặt so ảnh video ⭐
GIAI ĐOẠN 5  ĐĂNG & ĐO          →  cửa 5: CTR ở 48h
GIAI ĐOẠN 6  NẠP NGƯỢC VÀO FILE
```

Không qua cửa thì **không sang giai đoạn sau**. Quay lại, hoặc bỏ đề tài.

---

# GIAI ĐOẠN 0 — CẦU & CUNG *(1-2 giờ)*

Đây là giai đoạn cả tháng vừa rồi làm thiếu: chỉ đọc **cung**, không có dữ liệu **cầu**.

### 0.1 ~~Tìm cầu — quét bình luận đối thủ~~ ⚠️ ĐÃ THỬ, KHÔNG HIỆU QUẢ
Chạy thử trên Predators (6.800 bình luận): **top 20 không có một yêu cầu đề tài nào** — toàn câu đùa và bắt lỗi. Bỏ khỏi bước bắt buộc. Chi tiết: `NGHIENCUU_CloneSwarm` mục 8.

*(Đổi lại nó cho hai thứ khác: khán giả ngách này chê văn AI công khai — một bình luận "the chatGPT writing is extremely noticeable" được **4.400 like**; và họ soi cả thumbnail để đoán AI.)*

### 0.2 Đo cung — đếm bầy clone
```
mcp__nexlev__youtube_search chính title ứng viên
→ đếm số kênh đã làm  →  xem ĐỈNH BẦY
```

### 🚪 CỬA 0
| Hỏi | Bỏ nếu |
|---|---|
| Có người thật sự xin đề tài này không? | không ai nhắc |
| Đỉnh bầy bao nhiêu? | **dưới 10K** |
| Có nằm trong bảng "đã thử & chết" không? | có |
| Đề tài này thuộc lane "về BẠN" không? | có — 0 cú nổ/4 tháng |

---

# GIAI ĐOẠN 1 — CHỈ TITLE *(~30 phút)*

> ## 🔴 SỬA 05/08/2026 — THUMBNAIL RA KHỎI GIAI ĐOẠN 1 HOÀN TOÀN
>
> Chủ nhắc **lần thứ hai** *(04/08 rồi 05/08)*: **"thumbnail nên làm sau khi kịch bản hoàn thành, chứ có mỗi title thì chưa đủ."**
>
> Bản sửa 04/08 mới tách *gen ảnh* ra khỏi giai đoạn 1 nhưng vẫn giữ *chấm concept* ở đây. Vẫn sai, cùng một lý do: **concept dựng từ mỗi cái title thì cũng là mò.** Title cho biết đề tài, không cho biết khoảnh khắc nào trong bài là mạnh nhất — mà thumbnail phải vẽ đúng khoảnh khắc đó.
>
> **Nay: giai đoạn 1 chỉ chốt TITLE. Không concept, không ảnh.** Toàn bộ thumbnail — cả concept lẫn ảnh — dồn về **giai đoạn 5a**, lúc đã có kịch bản chốt + 200+ ảnh đúng style để lấy khối nhân vật.
>
> **Đổi lại phải bù cửa lọc đề tài** *(việc mà cửa 1 cũ đang gánh)*: nay nằm ở **cửa 0** — tra bầy clone live theo skill `chon-de-tai-trong-ngach`, và ở **cửa 2** — rubric kịch bản. Đề tài dở bị giết ở đó, trước khi gen ảnh.

### 1.1 Title
`HE_THONG_KichBan_v2` PHẦN C. Bảng 159 video xếp theo view trung bình của từng khuôn:

| Khuôn | Số video | View TB |
|---|---|---|
| `When Did Ancient Humans First ___?` | 8 | **287.075** ← kênh CHƯA dùng lần nào |
| `The [Extreme] Things Ancient Humans Did…` | 6 | 182.183 |
| `Why Did/Do Ancient Humans…` | 41 | 154.442 |
| `What Did Ancient Humans Do [When/During]…` | 13 | 113.807 |
| `How Did Ancient Humans [Verb] [Noun]?` | 42 | 101.957 |
| ⛔ `Did/Were/Could Ancient Humans…?` | 18 | 33.672 — thấp nhất, tránh |

⚠️ Cái quyết định **không phải** khoảnh-khắc-hay-cơ-chế *(khuôn cơ chế 287K đứng trên mọi khuôn khoảnh khắc)*, mà là **có nối được với hành vi hôm nay của người xem hay không**.

### 🚪 CỬA 1 — TITLE *(~5 phút)*
```
1. Dùng khuôn nào trong bảng?  (tránh Did/Were/Could)
2. Có nối vào một việc người xem VẪN LÀM hôm nay không?
3. Ngôi thứ ba — không rơi lane "về BẠN" (0 cú nổ / 4 tháng)?
4. Có khẳng định một SỰ THẬT nào chưa xác minh không?  → có thì bỏ,
   không đặt fact chưa tra vào title
5. Dưới 60 ký tự?
```

---

# GIAI ĐOẠN 2 — KỊCH BẢN *(1-2 ngày, viết theo ĐỢT)*

Luật ngôn ngữ: chủ duyệt qua **bảng EN + cột dịch VI**, không có cột hình lúc duyệt.

### 🚧 HAI CỔNG MÁY MÓC — chạy TRƯỚC khi phác xương chương *(mới 06/08/2026)*

> **Vì sao thêm.** Xương chương V19 bản đầu có **4 lỗi**, và chỉ lộ ra vì chủ bảo "check lại".
> Cả bốn đều bắt được bằng thao tác máy móc đã bỏ qua. Không có cổng thì lần sau lại thế.

**CỔNG A — CHỐNG GIẪM VIDEO CŨ.** Với **từng mỏ neo** định dùng, grep kịch bản **2-3 video
gần nhất**. Có kết quả → mỏ neo đó **chết**, hoặc tối đa nhắc lướt một câu, cấm làm thành khối.

```bash
cd "…/Build Channel Người Que Cổ Đại"
# PHẢI quét cả thư mục _nhap/ — bản nháp chứa câu đã bị cắt khỏi bản cuối,
# mà mình vẫn có thể vô tình viết lại đúng câu đó
grep -rniE "từ_khoá_mỏ_neo_1|từ_khoá_2|tên_tác_giả|con_số_đặc_trưng" \
  Video1[5-8]*/ --include="*.txt" --include="*.md"
```

⚠️ **Sót đã xảy ra 06/08:** cổng A bản đầu chỉ quét `Script_*_narration.txt`. Đoạn KẾT V19
viết ra trùng hai chỗ với V17 — một câu nằm trong `_nhap/Script_V17_FULL_v2.md` *(bản nháp,
đã bị cắt khỏi bản cuối)*, một thủ pháp nằm trong narration cuối. Quét thiếu thư mục nháp
là quét thiếu một nửa kho.

*Đã dính:* xương V19 bản đầu dựng cả một khối quanh Hadza-18-phút — **V18 đã xài trọn**,
kể cả cú lật cuối *(tuổi tác đổi đồng hồ sinh học)*. Chỉ một lệnh grep là ra.

**CỔNG B — KHÔNG CHÉP SỐ, TỰ TÍNH LẠI.** Mọi con số trong file `CHOT_` phải **tính lại từ
nguồn gốc**, kể cả số chính mình vừa ghi hôm qua. Đổi độ dài / đổi phạm vi là mọi con số
dẫn xuất đều mục nhưng **không tự đổi theo**.

| Số | Tính lại từ |
|---|---|
| Số từ | độ dài × **nhịp đọc thật đo từ video gần nhất** *(không dùng 140 wpm trong skill — kênh đang chạy ~178)* |
| Số ảnh | độ dài ÷ giây/ảnh — rồi **nhân ngược lại kiểm**: số ảnh × giây/ảnh có ra đúng độ dài không? |
| Ký tự title | `printf '%s' "…" \| wc -c`, không đếm bằng mắt |
| Mọi số trích từ paper | **mở bài gốc đọc**, không tin đoạn tóm tắt của công cụ tìm kiếm |

*Đã dính:* `CHOT_V19` ghi "3.200–3.800 từ · ~530 ảnh" — sót từ bản 20–25 phút, mà 530 ảnh ×
2,5 giây = **22 phút** chứ không phải 10. Suýt gen thừa hơn 250 ảnh. Cùng file ghi title
"58 ký tự", đếm máy ra **62**. Và con số Ethiopia đọc từ snippet là *"một nửa số người chết"*,
mở bài gốc ra mới thấy là *"một nửa số VỤ TẤN CÔNG"*.

| Đợt | Nội dung | Model |
|---|---|---|
| 1 | HOOK + setup → dừng duyệt | **Fable 5** (hook 15s đầu) |
| 2..n | 1-2 chương mỗi đợt → dừng duyệt | **Opus 5** |
| cuối | khúc mặt tối + KẾT | **Fable 5** (đoạn kết) |

**Bám:** `HE_THONG_KichBan_v2` — PHẦN B 7 luật · PHẦN F cơ học hook (câu hỏi lõi **trước giây 31**) · PHẦN G kết bài triết học (câu cuối phải chứa **vật thể cụ thể**).
**Nguyên liệu:** `KHO_AnDu_TruyenChem_LachLuat.md` · `NganHang_ReHook_BucketBrigade.md`

### 🔬 QA TỪNG CHƯƠNG — làm NGAY sau khi viết xong mỗi chương *(mới 05/08/2026)*

> **Vì sao thêm.** V18 viết trọn một chương rồi phải **cắt bỏ hoàn toàn** vì cây cầu lập luận sai *(suy từ "khác nhau giữa các cá thể" sang "thay đổi trong cùng một cá thể")*. Lỗi đó chỉ lộ ra ở vòng rubric cuối. Ba lỗi khác của V18 — danh từ kết trừu tượng, "low end of the range" không kéo theo "ngủ ít hơn bạn" — cũng đều bị bắt ở cuối, sau khi đã viết xong hết.
>
> Chương viết xong mà sai thì **phải viết lại cả chương**. QA ngay thì sửa một câu.

Chương nào viết xong, chấm ngay 5 mục dưới **trước khi viết chương kế**. ~5 phút/chương.

```
1. MỎ NEO   — mọi dữ kiện trong chương này đã tra web xác minh chưa?
              (tên · năm · tạp chí · địa danh). Chưa xác minh → CẮT hoặc
              hạ xuống "some evidence suggests". Không để nợ tới cuối.

2. LOGIC    — kết luận của chương có THẬT SỰ suy ra từ bằng chứng của nó không?
              Hỏi ngược: "bằng chứng này còn giải thích được điều gì khác?"
              Bẫy đã dính: giữa-cá-thể ≠ trong-một-cá-thể · tương quan ≠ nhân quả

3. GIỌNG AI — quét 14 dấu hiệu của skill `chong-van-ai-narration-en`.
              Đọc to. Câu nào "không người dẫn nào nói thế" → viết lại.

4. LUẬT CỨNG— dấu "!" = 0 · không "I" · không em-dash · không chấm phẩy ·
              câu cụt xen câu dài (không để mọi câu dài đều nhau)

5. TÒ MÒ    — chương này có MỞ một vòng tò mò mới TRƯỚC khi đóng vòng cũ không?
              Đọc câu cuối chương: nó có khiến người ta muốn nghe tiếp không,
              hay nghe như "xong rồi"?
```

⚠️ **QA từng chương KHÔNG thay được cửa 2.** Bốn thứ chỉ đo được trên toàn bài: nhịp tổng (từ/phút) · mật độ câu hỏi (1 câu mỗi 60-90 giây) · callback về hook · cân đối độ dài giữa các chương. Cửa 2 vẫn chạy đủ.

### 🚪 CỬA 2 *(toàn bài)*
```
1. Rubric ≥ 62/72, không mục nào = 0
2. VERIFY MỌI MỎ NEO bằng web search (tên · năm · tạp chí · địa danh)
   — tỉ lệ sai của dữ liệu NotebookLM đang là 7/10
3. Chạy skill chống-văn-AI trên TOÀN BÀI, đọc to từng câu
4. Đo nhịp: từ/phút · mật độ câu hỏi · câu hỏi lõi trước giây 31 ·
   danh từ câu cuối có phải VẬT THỂ CỤ THỂ không
5. (tuỳ) đưa GPT 5.6 review → phân loại áp ngay / áp có sửa / bỏ + lý do
```

---

# GIAI ĐOẠN 3 — SHOT & ẢNH

Skill `chia-shot-va-prompt-anh`. Nhịp ~8-10 từ/shot.

### 🚪 CỬA 3
```
1. Gen vào THƯ MỤC RỖNG, MỘT lượt
2. Đếm file == số prompt   (bộ đếm của tool chạy lệch nếu gen 2 đợt)
3. Không thiếu số, không dư số, không file <20KB
4. Đo độ sáng nền từng ảnh, đối chiếu nền đã chỉ định
```

---

# GIAI ĐOẠN 4 — TIẾNG & GHÉP

**Dùng app GhepVideo Studio. TUYỆT ĐỐI không tự viết script ghép** — bản web cũ có lỗi sắp xếp `.mp3` đã làm hỏng V15 và đăng công khai.

### 🚪 CỬA 4 — ba thứ KHÔNG chứng minh được gì: chạy không lỗi · đủ số file · đúng độ dài
```
1. Lấy tiếng ở 4 mốc rải đều → đối chiếu đường bao âm lượng với mp3 đúng số
   ĐẠT: khớp ≥0.90 · đối chứng ≤0.65
2. Lấy khung hình ở 5 mốc → so với ảnh đúng số
   ĐẠT: lệch ≤0.5 · ảnh sai ≥10
3. số ảnh == số tiếng == số shotline
```

---

# GIAI ĐOẠN 5a — THUMBNAIL THẬT ⭐ *(chuyển xuống đây 04/08/2026)*

**Chỉ làm SAU khi đã có đủ ảnh video.** Lúc này trong tay đã có:
- **N ảnh đúng style**, khối nhân vật đã khoá và đã chạy qua cả video
- kịch bản chốt — biết khoảnh khắc nào mạnh nhất
- video ghép xong để soi

Dựng thumbnail lúc này chỉ còn là **gom lại**, 1-2 vòng. Làm ở giai đoạn 1 thì phải mò, vì chưa có gì để đối chiếu.

### Cách làm
```
1. Lấy Y NGUYÊN khối nhân vật trong build_prompts.py của chính video đó
   -> thumbnail va video chac chan cung mot nhan vat
2. Bo cuc: lay canh manh nhat trong kich ban, hoac mot khung dep san trong N anh
3. Dan chu theo concept da chot o CUA 1
```

### 🚪 CỬA 5a — 3 cửa thật *(xem `TEMPLATE_Thumbnail_KHOA_v1.md`)*
```
1. CHẤT LIỆU  — nét vẽ tay, màu xỉn (bão hoà 24-31). Sạch như vector là trượt
2. TRẮNG ĐẶC  — mặt/tay/chân 3-10% khung
3. CẮT MẶT    — đặt cạnh mặt trong ẢNH VIDEO cùng cỡ: có cùng một nhân vật không?
   (+ cạnh mặt đối thủ: có cùng một họ không?)
Cộng thêm: thu về 168×94 còn đọc được hết không.
```

⚠️ **Độ sáng · bão hoà · chiều cao chữ KHÔNG phải cửa** — đo 29 quả thắng, tương quan với view lần lượt −0,10 · −0,09 · +0,01.

---

# GIAI ĐOẠN 5 — ĐĂNG & ĐO

Metadata theo skill `youtube-metadata`. Verify title lần cuối ngay trước đăng (bầy clone mọc theo giờ).

### 🚪 CỬA 5 — đo ở 48h, chỉ nhìn MỘT con số
| CTR | Nghĩa | Làm gì |
|---|---|---|
| **< 2%** | thumbnail hỏng | **thay thumbnail ngay**, giữ nguyên video |
| **2-4%** | chưa đạt | thay thumbnail, đổi 1 biến duy nhất |
| **≥ 4%** | qua cửa | giờ mới xét retention |

⚠️ **Chỉ đổi MỘT biến mỗi lần thay** — không thì không biết cái gì có tác dụng.
⚠️ Lượt hiển thị thấp **không** nghĩa là YouTube không đẩy — kênh đã có 17.000 hiển thị. Nó đẩy rồi, người ta không bấm.

---

# GIAI ĐOẠN 6 — NẠP NGƯỢC VÀO FILE

Sau mỗi video, cập nhật:
- `BANG_CAU_TatCa_CuNo` — đề tài + kết quả thật của mình
- `PROMPT_TONG_Thumbnail_v6` — thumbnail này CTR bao nhiêu, khuôn nào
- `00_LUAT_HIEN_HANH` — nếu có luật bị bác

**Luật ghi chép:** bác luật nào thì **dán biển ⛔ ngay lên file chứa luật đó** — file mới không tự bò sang file cũ. Và soát cả `~/.claude/skills/` + `memory/MEMORY.md`, vì đó là hai chỗ gọi file vào mà không ai nhớ.

---

# PHÂN VAI

| Ai | Việc |
|---|---|
| **Chủ** | duyệt kịch bản qua cột VI · gen ảnh + thumbnail (ChatGPT/Flow) · bấm ghép trong app · đăng · chụp số Studio |
| **Claude** | chọn đề tài (tra cầu + đếm bầy) · viết kịch bản · concept thumbnail · verify mỏ neo · chạy 5 cửa kiểm · nạp ngược vào file |
| **NotebookLM** | **cách viết** — ẩn dụ, truyện chêm, kết bài, lách kiểm duyệt. **KHÔNG dùng để chọn đề tài** — nó không thấy YouTube live nên mù hoàn toàn về cung |
| **GPT 5.6** | review chéo kịch bản. Không áp mù feedback |

---

# NHỊP ĐĂNG

Ngách này là **xổ số** — Pastly đăng 8 video cùng ngày, kết quả từ 854 tới 146.000. Tỉ lệ trúng ~1/9. Kênh mình 11 video / 0 trúng là kết cục có xác suất 27% — bình thường.

→ **Không chờ hoàn hảo. Đăng đều.** Nhưng đừng đăng thứ chưa qua cửa 1 — đó không phải mua thêm vé số, đó là mua vé đã bị gạch.

**Điều bất thường thật sự không phải chưa có cú nổ, mà là trung vị của những quả trượt:** Pastly trượt vẫn ~900 view, mình trượt còn 15. Sàn đó chính là CTR.
